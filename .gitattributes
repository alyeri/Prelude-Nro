# Fichiers embarques dans le romfs et poses tels quels sur la SD : AUCUNE
# conversion de fin de ligne (byte-fidelite = patches .ips, exefs.nsp, certs, mitm.lst).
romfs/sd/** -text

# Unified diff context uses a single space for blank lines by specification.
skyline/*.patch whitespace=-blank-at-eol
